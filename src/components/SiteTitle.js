import React from 'react';

const SiteTitle = () => (
  <h1
    data-testid="site-title"
    style={{
      fontSize: '2rem',
      fontWeight: '600',
      marginBottom: '2rem',
      lineHeight: '1em',
      color: 'orangered',
      textAlign: 'center',
    }}
  >
    Bookstore CMS
  </h1>
);

export default SiteTitle;
