import React from 'react';

const SiteTitle = () => (
  <h1
    data-testid="site-title"
    style={{
      fontSize: '2rem',
      fontWeight: '600',
      marginBottom: '2rem',
      lineHeight: '1em',
      color: '#3c6c8c',
      textAlign: 'center',
    }}
  >
    Bookstore CMS
  </h1>
);

export default SiteTitle;
