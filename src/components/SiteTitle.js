import React from 'react';

const SiteTitle = () => (
  <h1
    data-testid="site-title"
    style={{
      fontSize: '3rem',
      fontWeight: '600',
      marginBottom: '2rem',
      lineHeight: '1em',
      color: '#ececec',
      textAlign: 'center',
    }}
  >
    Bookstore CMS
  </h1>
);

export default SiteTitle;
